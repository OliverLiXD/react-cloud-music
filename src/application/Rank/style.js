import styled from "styled-components";

export const Container = styled.div`
  position: fixed;
  top: 26rem;
  bottom: 0;
  width: 100%;
  .offical, .global {
    font-size: 4rem;
    font-weight: 700;
    padding: 1rem;
    padding-top: 3rem;
  }
`